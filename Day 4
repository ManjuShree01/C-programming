//Swap
#include <stdio.h>
int a,b;
void swap(int *a,int *b)
{
    int temp=*a;
    *a=*b;
    *b=temp;
}
int main()
{
    printf("Enter 2 no:\n");
    scanf("%d %d",&a,&b);
    swap(&a,&b);
    printf("The swapped numbers are a = %d and b = %d.",a,b);
    return 0;
}
